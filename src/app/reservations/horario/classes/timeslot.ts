export class Timeslot {
    idTimeslot ? : number
    Timeslot_date ? : any
    Begin_time ? : any
    End_time ? : any
    Exam_group ? : number
    Max_Num_students ? : number
    number_Reservations ? : number
    Exam_type_idExam_type ? : number
    Exam_type_name ? : string
    Exam_center_idExam_center ? : number
}
